luaz中每一个表和用户数据类型都具有各自独立的元表，其他类型的值则共享对应数据类型的同一个元表。（字符串有共用元表，其他类型在
默认情况下没有元表)


* 获取元表：getmetatable
* 设置元表：setmetatable

## 表相关的元方法
### 访问元表(__index)
当访问不存在时，如果存在这个元方法，则由该元方法提供结果。

### 修改不存在的字段(__newindex)
当对表中不存在的索引赋值时，解释器就会查找__nexindex方法，如果该方法存在，那么解释器就调用它而不执行赋值。